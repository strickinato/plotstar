{
  "name": "plotstar",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "develop": "elm-live src/Main.elm --dir=public --pushstate -- --debug --output=public/elm.js",
    "build": "elm make src/Main.elm --output=public/elm.js",
    "start": "yarn build && serve --single public"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-live": "^4.0.2",
    "serve": "^11.3.0"
  }
}
